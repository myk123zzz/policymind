FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml uv.lock ./
COPY src/ src/

RUN pip install uv && uv sync --no-dev
CMD ["uv", "run", "arq", "policymind.workers.ingestion_worker.WorkerSettings"]
