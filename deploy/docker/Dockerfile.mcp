FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml uv.lock ./
COPY src/ src/

RUN pip install uv && uv sync --no-dev
EXPOSE 8100
CMD ["uv", "run", "python", "-c", "from policymind.mcp.server import run_stdio_server; run_stdio_server()"]
