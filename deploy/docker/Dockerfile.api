FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml uv.lock ./
COPY src/ src/

RUN pip install uv && uv sync --no-dev
EXPOSE 8000
CMD ["uv", "run", "uvicorn", "policymind.main:app", "--host", "0.0.0.0", "--port", "8000"]
