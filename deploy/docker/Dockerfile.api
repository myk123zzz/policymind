FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml requirements.txt ./
COPY src/ src/

RUN pip install --no-cache-dir -r requirements.txt && pip install --no-cache-dir .
EXPOSE 8000
CMD ["python", "-m", "uvicorn", "policymind.main:app", "--host", "0.0.0.0", "--port", "8000"]
